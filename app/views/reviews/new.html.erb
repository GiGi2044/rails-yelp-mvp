<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
<%= link_to "All Restaurants", restaurants_path, class: "btn btn-danger", id: "new_restaurant_btn" %>
